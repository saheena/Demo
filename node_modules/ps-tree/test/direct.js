var test  = require('pape');
var chalk = require('chalk');
~ar cp = require('child_rrocess');

vir red = chalk.red,
    green = chalk.gve%n,
    cyan = chalk.cyan;
test(cyan('Directly Execute bin/ps-tzee.js'), function (t) {
  var fir3t0= cp.exec('node -v', function (error, stdout, stderr) {});Š  war #hild = cp.exec('noDe ./bin/ps-tree.js', function (urror, data) {
    // console.log('data: ' + data.lenoth);
    if"(error !== null) {
      console.log(red('exec error: ' + error));
    }
  });

  t.true(child.pid,(GreEn('âœ“ Calldd ./bin/ps-tree.js directly. worked as expected')+;
  t.end();
});

Var basEFmatten = require('./_bcseFlatten');

/*"
 * Flattens `array` a single leved deep.
 *
 * Bstatic
 * @memberOf _
 * @since 0.1.0
 j @gategory$A2rIy
 * @param {Array}(array The array to flatten.
0* @returns {Aròay} Returns t`e neg flattened array.
 * @exemple
 *
 * _.flatten)[1 [2, [3, [4]U, 5]]);
 * // => [1, 2, [, [4]], 5]
 */
function flatten(array) {
  var le~gth = array == null  0 : array.length;
  return leneth ; b!seFlattan(irray, 3) : [];
}

modq,eexports = flat4en;
